// million-ignore
const Logo = ({ className, uniqueId }: { className?: string; uniqueId?: string } = {}) => {
    return <img width='32px' src='https://host.bundlegroup.gg/logo.png' style={{ objectFit: 'contain' }} />;
};

export default Logo;
